# AutoUpgradeEscrow Audit Notes

## Contract Role

`AutoUpgradeEscrow` is the dedicated escrow contract for auto-upgrade funds in the F-Freedom Program.

It holds USDT tagged by:

```txt
user
fromLevel
toLevel